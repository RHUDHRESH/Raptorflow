{
  "name": "raptorflow",
  "private": true,
  "scripts": {
    "dev": "npm --prefix raptorflow-app run dev",
    "build": "npm --prefix raptorflow-app run build",
    "start": "npm --prefix raptorflow-app run start",
    "lint": "npm --prefix raptorflow-app run lint",
    "test": "npm --prefix raptorflow-app run test"
  },
  "dependencies": {
    "pg-sdk-node": "^2.0.2"
  }
}
