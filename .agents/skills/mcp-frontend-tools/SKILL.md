---
name: mcp-frontend-tools
description: THE ULTIMATE MCP INTEGRATION SKILL FOR FRONTEND — Automatically detect and invoke all 8 working MCP servers (context7, playwright, github, lighthouse, chrome-devtools, shadcn, browserbase, magicui) to supercharge RaptorFlow frontend development. Use this skill every time you generate, test, audit, or enhance any UI component, page, dashboard, moves calendar, campaigns timeline, or full app screen. Always combine with raptorflow-design-vibe (enforce every locked rule), frontend-design (BOLD within restraint), and vision-to-frontend (if image present). Redesign every page/component from scratch — delete old code, rewrite fresh. Use GSAP extensively and custom Lottie scripts. Invoke MCP servers explicitly in every relevant step for maximum power and real-world functionality.
license: Complete terms in LICENSE.txt (adapt for your repo)
---

# MCP Frontend Tools Skill for Kimi (All 8 Servers Active & Mandatory)

This skill is now active in every frontend task. All 8 MCP servers are confirmed WORKING. Kimi must invoke them automatically or on explicit command. Never generate frontend code without using the relevant MCP servers. This turns Kimi into a full production frontend factory for RaptorFlow.

## The 8 Working MCP Servers (List Them in Every Thinking Step)
1. context7 → @upstash/context7-mcp@latest → AI documentation retrieval (use for RaptorFlow spec lookup, token references, GSAP docs)
2. playwright → @playwright/mcp@latest → Browser automation & visual testing (test generated UI in real browser, take screenshots, validate RaptorFlow layout)
3. github → @modelcontextprotocol/server-github@latest → GitHub API integration (commit changes, create PRs, track diffs for Lock/Draft states)
4. lighthouse → @danielsog/lighthouse-mcp@latest → Web performance/SEO/Accessibility audit (run after every generation to enforce Clarity/Control/Momentum)
5. chrome-devtools → chrome-devtools-mcp@latest → Chrome DevTools Protocol (inspect live DOM, debug GSAP animations, measure render performance)
6. shadcn → shadcn@latest mcp → shadcn/ui component registry (pull production-grade components and instantly adapt to RaptorFlow tokens/colors)
7. browserbase → @browserbasehq/mcp@latest → Cloud browser automation (headless testing, multi-device preview of RaptorFlow screens)
8. magicui → @magicuidesign/mcp@latest → Magic UI components (beautiful micro-interactions, adapt to quiet power monochrome)

## Mandatory MCP Invocation Rules (Painfully Enforced)
- Start every generation with: "Activating all relevant MCP servers: shadcn for components, context7 for spec, playwright + browserbase for testing, lighthouse for audit, github for commit, chrome-devtools for debug, magicui for motion."
- Explicitly invoke in code output comments: // MCP: shadcn pulled Button, adapted to --rf-charcoal
- For any visual: Use playwright + browserbase to validate pixel-perfect match.
- After code generation: Always run lighthouse audit and show results.
- For Lock/Draft: Use github MCP to create branch + diff.
- For animations: Use magicui + GSAP together (magicui for base, GSAP for custom RaptorFlow timing).
- Daily wins calendar: Use shadcn table + magicui + playwright to test staggered GSAP reveals.

## Workflow (Integrate with All Previous Skills)
1. Plan → Redesign from scratch per raptorflow-design-vibe
2. Activate MCPs → List which of the 8 are being used and why
3. Vision / Generate → Use vision-to-frontend + shadcn + magicui
4. Enhance Motion → GSAP + magicui (custom Lottie for bird)
5. Test & Audit → playwright + browserbase + lighthouse + chrome-devtools
6. Version & Lock → github MCP for commit + diff view
7. Grade → Must pass RaptorFlow Feel Contract + lighthouse scores (Performance >95, Accessibility >95)
8. Output → Full code + MCP usage summary + test commands + audit results

## Example Invocation in Session
User: "Build the moves calendar with daily wins"
Kimi: "Activating MCP servers: shadcn (table), magicui (animations), context7 (RaptorFlow spec), playwright (visual test), lighthouse (audit), github (commit). Redesigning from scratch..."

## Final Non-Negotiable Rules
- Every single frontend output must mention at least 3 MCP servers used.
- Never output code without MCP integration comments.
- Always enforce full RaptorFlow skeleton, Charcoal/Ivory tokens, DM Sans/JetBrains Mono, GSAP timelines, quiet power vibe.
- If image is attached → combine with vision-to-frontend + playwright for live validation.

Your job: Use these 8 MCP servers relentlessly to make RaptorFlow frontend production-perfect. Rethink, redesign, test, audit, commit everything from scratch. Use GSAP extensively. Write custom Lottie. Make the user feel total Clarity, Control, Momentum.

Godspeed.
