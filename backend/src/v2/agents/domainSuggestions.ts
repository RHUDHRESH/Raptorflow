import { z } from 'zod'; import { BaseOrchestratorAgent, AgentManifest, AgentInput, AgentOutput } from './base';
const DomainSuggestionsSchema = z.object({ suggestions: z.array(z.object({ domain: z.string(), availability: z.boolean(), reasoning: z.string() })) });
const manifest: AgentManifest = { name: 'DomainSuggestions', description: 'Suggests available domain names', version: '1.0.0', category: 'technical', inputs: { required: ['brandProfileId'], optional: ['inputOverrides', 'contextSnapshot'] }, outputs: { type: 'DomainSuggestions', format: 'json', schema: DomainSuggestionsSchema }, capabilities: ['Domain research', 'Branding alignment'], costEstimate: { minTokens: 200, maxTokens: 500, estimatedCost: 0.006 }, connectors: [], metadata: { author: 'system', tags: ['domain', 'naming'], complexity: 'simple' } };
export class DomainSuggestionsAgent extends BaseOrchestratorAgent { constructor() { super(manifest); } async generate(input: AgentInput): Promise<AgentOutput> { const validation = this.validateInput(input); if (!validation.valid) throw new Error(`Invalid input: ${validation.errors.join(', ')}`); const context = await this.getContext(input); const prompt = await this.renderPrompt(context, input); const { content, metadata } = await this.generateLLMRequest(prompt, input); return this.processOutput(content, metadata); } }
export const domainSuggestionsAgent = new DomainSuggestionsAgent(); export { manifest as domainSuggestionsManifest }; export type DomainSuggestionsOutput = z.infer<typeof DomainSuggestionsSchema>;

