import { z } from 'zod'; import { BaseOrchestratorAgent, AgentManifest, AgentInput, AgentOutput } from './base';
const PDFGeneratorSchema = z.object({ title: z.string(), content: z.string(), sections: z.array(z.object({ title: z.string(), content: z.string() })) });
const manifest: AgentManifest = { name: 'PDFGenerator', description: 'Creates structured PDF content', version: '1.0.0', category: 'content', inputs: { required: ['brandProfileId'], optional: ['inputOverrides', 'contextSnapshot'] }, outputs: { type: 'PDFGenerator', format: 'json', schema: PDFGeneratorSchema }, capabilities: ['Content structuring', 'PDF generation'], costEstimate: { minTokens: 400, maxTokens: 1000, estimatedCost: 0.012 }, connectors: ['S3'], metadata: { author: 'system', tags: ['pdf', 'content', 'generation'], complexity: 'medium' } };
export class PDFGeneratorAgent extends BaseOrchestratorAgent { constructor() { super(manifest); } async generate(input: AgentInput): Promise<AgentOutput> { const validation = this.validateInput(input); if (!validation.valid) throw new Error(`Invalid input: ${validation.errors.join(', ')}`); const context = await this.getContext(input); const prompt = await this.renderPrompt(context, input); const { content, metadata } = await this.generateLLMRequest(prompt, input); return this.processOutput(content, metadata); } }
export const pdfGeneratorAgent = new PDFGeneratorAgent(); export { manifest as pdfGeneratorManifest }; export type PDFGeneratorOutput = z.infer<typeof PDFGeneratorSchema>;

