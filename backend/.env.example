# BACKEND ENV EXAMPLE (Reconstruction Mode)
#
# Canonical backend: FastAPI app created by `backend/app_factory.py`.
# Auth + payments are intentionally removed.
# Tenant boundary is workspace id passed via `x-workspace-id` header.

# --- Runtime ---
ENVIRONMENT=development
DEBUG=true
HOST=0.0.0.0
PORT=8000

# Keep legacy path rewrite enabled so `/api/v1/*` continues to work (rewritten to `/api/*`).
ENABLE_LEGACY_API_PATHS=true
ENABLE_LEGACY_V1=false

# --- Supabase (Database) ---
# Backend uses service role key for all DB calls (no Supabase Auth / no browser client).
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# --- Muse / Vertex AI (Optional) ---
# Required for `/api/muse/generate`.
VERTEX_AI_PROJECT_ID=your_gcp_project_id
VERTEX_AI_LOCATION=us-central1
VERTEX_AI_MODEL=gemini-2.0-flash-exp
# Standard GCP auth method:
GOOGLE_APPLICATION_CREDENTIALS=./gcp-service-account.json

# --- Monitoring (Optional) ---
SENTRY_DSN=
