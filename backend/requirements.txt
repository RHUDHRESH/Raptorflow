# RaptorFlow Backend — Production Requirements
# Canonical services: Supabase, Vertex AI, Upstash Redis, Resend, Sentry

# Core Framework
fastapi>=0.104,<1.0
uvicorn[standard]>=0.24,<1.0
pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0

# Supabase (Database + Storage)
supabase>=2.3,<3.0

# Upstash Redis (Caching)
upstash-redis>=1.1,<2.0

# Vertex AI (Inference)
google-cloud-aiplatform>=1.38,<2.0
vertexai>=1.0

# Email (Resend)
resend>=2.4,<3.0
jinja2>=3.1,<4.0

# Monitoring (Sentry)
sentry-sdk[fastapi]>=1.40,<2.0
structlog>=23.2,<24.0

# HTTP Client
httpx>=0.25,<1.0

# Security
python-jose[cryptography]>=3.3,<4.0
passlib[bcrypt]>=1.7,<2.0
python-multipart>=0.0.6,<1.0
cryptography>=41.0,<42.0

# Utilities
python-dotenv>=1.0,<2.0
orjson>=3.9,<4.0
tenacity>=8.2,<9.0
python-dateutil>=2.8,<3.0
