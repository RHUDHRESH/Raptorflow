# Documentation Archive

This folder contains historical reports, PR bodies, older phase specs, and source
PDFs. These files are preserved for context and traceability only.

Do not use this folder as the source of truth for setup, deployment, route
coverage, infrastructure, or current product behavior. Use the active docs in
`docs/`, `docs/canonical/`, and `docs/runbooks/` instead.
