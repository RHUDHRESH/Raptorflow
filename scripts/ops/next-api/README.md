# Archived Next API Routes

These routes were removed from the Next.js runtime and moved here as internal ops/dev helpers.
They should not be exposed in production.

If you need to resurrect one, copy it back under `src/app/api/` and secure it before use.
