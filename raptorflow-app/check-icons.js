import icons from '@hugeicons/react';
const iconNames = Object.keys(icons);
console.log(
  'Flash matches:',
  iconNames.filter((n) => n.includes('Flash'))
);
console.log(
  'Lightning matches:',
  iconNames.filter((n) => n.includes('Lightning'))
);
console.log(
  'Dashboard matches:',
  iconNames.filter((n) => n.includes('Dashboard'))
);
console.log(
  'Settings matches:',
  iconNames.filter((n) => n.includes('Settings'))
);
console.log(
  'Package matches:',
  iconNames.filter((n) => n.includes('Package'))
);
console.log(
  'Layers matches:',
  iconNames.filter((n) => n.includes('Layer'))
);
console.log(
  'User matches:',
  iconNames.filter((n) => n.includes('User'))
);
console.log(
  'Mega matches:',
  iconNames.filter((n) => n.includes('Mega'))
);
console.log(
  'Home matches:',
  iconNames.filter((n) => n.includes('Home'))
);
console.log(
  'Sparkle matches:',
  iconNames.filter((n) => n.includes('Sparkle'))
);
