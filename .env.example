# RAPTORFLOW - ENV EXAMPLE (Reconstruction Mode)
#
# Copy to:
# - `.env.local` for Next.js
# - `.env` for FastAPI backend
#
# Auth + payments are intentionally removed in reconstruction mode.
# Tenant boundary is the workspace id (`x-workspace-id` header).

# --- Frontend / Proxy (Next.js) ---
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Backend base URL used by the Next.js proxy route (`src/app/api/[...path]/route.ts`)
# Defaults to http://localhost:8000 if unset.
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
BACKEND_API_URL=http://localhost:8000

# --- Backend runtime (FastAPI) ---
ENVIRONMENT=development
DEBUG=true
HOST=0.0.0.0
PORT=8000

# Keep legacy path rewrite enabled so `/api/v1/*` continues to work (it rewrites to `/api/*`).
ENABLE_LEGACY_API_PATHS=true
ENABLE_LEGACY_V1=false
ENABLE_LEGACY_EXPERIMENTAL_ROUTES=false

# --- Supabase (Database) ---
# Backend uses service role key for all DB calls (no browser Supabase client).
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# --- Vertex AI (Optional; required for Muse generation) ---
VERTEX_AI_PROJECT_ID=your_gcp_project_id
VERTEX_AI_LOCATION=us-central1
VERTEX_AI_MODEL=gemini-2.0-flash-exp
GOOGLE_APPLICATION_CREDENTIALS=./gcp-service-account.json

# --- Redis (Optional) ---
REDIS_URL=redis://localhost:6379
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# --- Monitoring (Optional) ---
SENTRY_DSN=
